class Sessions {
  constructor(
    sessionTitleHome,
    sessionButton,
    sessionTitle,
    sessionDescription
  ) {
    this.sessionTitleHome = sessionTitleHome;
    this.sessionButton = sessionButton;
    this.sessionTitle = sessionTitle;
    this.sessionDescription = sessionDescription;
  }
}

export default Sessions;
